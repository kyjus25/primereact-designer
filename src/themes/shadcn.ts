import type { PrimeReactPTOptions } from 'primereact/api';
import { classNames } from 'primereact/utils';

export default {
  button: {
    root: classNames('bg-red-500'),
  },
} as PrimeReactPTOptions;
